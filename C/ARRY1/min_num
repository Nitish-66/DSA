//find the  min  number of an array 
#include<stdio.h>

int main()
{
    int i,arr[10],min;

    printf("Enter the numbers : ");
    for ( i = 0; i < 5; i++)
    {
        scanf("%d",&arr[i]);
    }
    min=arr[0];
    for ( i = 0; i < 5; i++)
    {
         if(arr[i]<max)
         max=arr[i];
    }
    printf("Min number is : %d ",min);
    
    return 0;
}